export interface Product {
    id: number,
    name: string,
    category: string,
    description: string,
    fullDescription: string,
    price: number,
    currency: string,
    slug: string,
    image: string
}